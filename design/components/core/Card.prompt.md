The system's only container primitive: warm cream surface, 1px oat border, 8px radius, never a shadow.

\`\`\`jsx
<Card>
  <h4>Resolve 65% of conversations</h4>
  <p style={{ color: "var(--text-muted)" }}>Fin answers instantly, in any language.</p>
</Card>
\`\`\`

Tones: cream (default), white, inverse (off-black). Set \`interactive\` for clickable cards — the border darkens to off-black rather than lifting.

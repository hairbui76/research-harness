The system's primary action element — near-rectangular (4px radius), with a physical scale(1.1) hover and scale(0.85) press.

\`\`\`jsx
<Button variant="primary">Start free trial</Button>
<Button variant="outlined" size="sm">View pricing</Button>
<Button variant="accent"><Icon name="sparkles" size={16} /> Ask Fin</Button>
\`\`\`

Variants: primary (off-black, inverts to white on hover), outlined, warm (cream card button), accent (Fin Orange — AI/brand moments only), ghost. Sizes sm/md/lg. Never increase the radius past 4px.
